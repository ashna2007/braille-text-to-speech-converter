[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "opencv-python"
version = "4.10.0.84"
description = "Metadata-only shim that installs headless OpenCV for cloud deployment"
requires-python = ">=3.10"
dependencies = ["opencv-python-headless==4.10.0.84"]

[tool.setuptools]
packages = []
