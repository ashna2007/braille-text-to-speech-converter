def translate_braille(recognized_text: str) -> str:
    """Return text from the current A-Z classifier.

    Prisha's Liblouis implementation can replace this function while keeping
    the same string-to-string interface.
    """

    return recognized_text.strip()
